# /etc/modprobe.d/nvidia-perf.conf
# Generated by neo-mittens ansible role `gaming_kernel`. Do not edit by hand;
# edit `nvidia_modprobe_options` in ansible/group_vars/gaming_desktops.yml.
#
# Loaded at module-load time. Requires initramfs regen + reboot to apply
# (handled automatically by the role's regenerate-initramfs handler).
{% for key, value in nvidia_modprobe_options.items() %}
options nvidia {{ key }}={{ value }}
{% endfor %}
