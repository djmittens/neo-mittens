# Secrets - never commit these!
.secrets.env
*.key
*.pem
credentials.json
.env.local

# Backup data directories (if stored in repo)
backup/data/
backup/snapshots/

# CI artifacts
ci/artifacts/
ci/cache/

# Local testing
*.local
test-*.sh
